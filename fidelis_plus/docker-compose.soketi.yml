services:
  soketi:
    image: quay.io/soketi/soketi:latest
    environment:
      SOKETI_DEBUG: "1"
      SOKETI_DEFAULT_APP_ID: "${PUSHER_APP_ID:-fidelis}"
      SOKETI_DEFAULT_APP_KEY: "${PUSHER_APP_KEY:-local}"
      SOKETI_DEFAULT_APP_SECRET: "${PUSHER_APP_SECRET:-local}"
    ports:
      - "6001:6001"
      - "9601:9601"

